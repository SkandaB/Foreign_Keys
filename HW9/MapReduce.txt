// Query to find the number of users from Germany

db.users.mapReduce(
    function() {emit(this.location, 1);},
    function(key,values) { return Array.sum(values)},
    {
        query: {location: "Germany"},
        out: "location_count"
    }
)

// Query to return all postIDs which are not answered

db.questions.mapReduce(
    function() {
    if(this.accepted_answer_id == null)
    emit(this.id, 1);
    },
    function(key,values) { return Array.sum(values)},
    {
        out: "unanswered_question_count"
    }
)